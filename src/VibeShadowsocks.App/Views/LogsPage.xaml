<Page
    x:Class="VibeShadowsocks.App.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24,20,24,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Uid="LogsTitle" Style="{ThemeResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Command="{Binding RefreshCommand}" ToolTipService.ToolTip="{Binding TooltipRefreshLogs}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                        <TextBlock x:Uid="RefreshBtnText" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding ExportDiagnosticsCommand}" ToolTipService.ToolTip="{Binding TooltipExportDiag}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xEDE1;" FontSize="14" />
                        <TextBlock x:Uid="ExportDiagBtnText" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="4">
            <TextBox
                Text="{Binding LogText}"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                IsReadOnly="True"
                FontFamily="Cascadia Mono,Consolas,Courier New"
                FontSize="12"
                BorderThickness="0"
                Background="Transparent" />
        </Border>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Opacity="0.6" FontSize="12" TextTrimming="CharacterEllipsis" />
    </Grid>
</Page>
