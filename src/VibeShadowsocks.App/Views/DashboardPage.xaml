<Page
    x:Class="VibeShadowsocks.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Spacing="12" Padding="16">
            <TextBlock Text="Status" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock Text="{Binding StateText}" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Connect" Command="{Binding ConnectCommand}" />
                <Button Content="Disconnect" Command="{Binding DisconnectCommand}" />
                <Button Content="Toggle" Command="{Binding ToggleCommand}" />
                <ProgressRing IsActive="{Binding IsBusy}" Width="24" Height="24" />
            </StackPanel>

            <TextBlock Text="{Binding ActiveServerText}" />
            <TextBlock Text="{Binding ActivePacText}" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="Update PAC" Command="{Binding UpdatePacCommand}" />
                <Button Content="Copy Diagnostics" Command="{Binding CopyDiagnosticsCommand}" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" />
            </StackPanel>

            <TextBox
                Header="Recent logs"
                Text="{Binding LogPreview}"
                MinHeight="180"
                AcceptsReturn="True"
                IsReadOnly="True"
                TextWrapping="Wrap" />
        </StackPanel>
    </ScrollViewer>
</Page>
