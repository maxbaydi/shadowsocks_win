<Page
    x:Class="VibeShadowsocks.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Spacing="16" Padding="24,20,24,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Uid="DashboardTitle" Style="{ThemeResource TitleTextBlockStyle}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Command="{Binding RefreshCommand}" ToolTipService.ToolTip="{Binding TooltipRefreshStatus}">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                </Button>
            </Grid>

            <Border
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Width="56" Height="56" CornerRadius="28"
                          Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}">
                        <FontIcon x:Name="StateIcon" Glyph="&#xEDAF;" FontSize="24"
                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                        <TextBlock Text="{Binding StateText}" Style="{ThemeResource SubtitleTextBlockStyle}" />
                        <TextBlock Text="{Binding StateMessage}" Opacity="0.7"
                                   TextTrimming="CharacterEllipsis" MaxLines="2" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <ProgressRing IsActive="{Binding IsBusy}" Width="28" Height="28" />
                        <Button
                            Style="{ThemeResource AccentButtonStyle}"
                            Command="{Binding ConnectCommand}"
                            IsEnabled="{Binding CanConnect}"
                            MinWidth="110">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE768;" FontSize="14" />
                                <TextBlock x:Uid="ConnectBtnText" />
                            </StackPanel>
                        </Button>
                        <Button
                            Command="{Binding DisconnectCommand}"
                            IsEnabled="{Binding CanDisconnect}"
                            MinWidth="120">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE71A;" FontSize="14" />
                                <TextBlock x:Uid="DisconnectBtnText" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE968;" FontSize="16" Opacity="0.7" />
                            <TextBlock x:Uid="ActiveServerLabel" Opacity="0.7" />
                        </StackPanel>
                        <ComboBox
                            ItemsSource="{Binding ServerProfiles}"
                            SelectedItem="{Binding SelectedServer, Mode=TwoWay}"
                            DisplayMemberPath="Name"
                            HorizontalAlignment="Stretch" />
                    </StackPanel>
                </Border>

                <Border
                    Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE774;" FontSize="16" Opacity="0.7" />
                            <TextBlock x:Uid="RoutingModeLabel" Opacity="0.7" />
                        </StackPanel>
                        <ComboBox
                            ItemsSource="{Binding RoutingModes}"
                            SelectedItem="{Binding SelectedRoutingMode, Mode=TwoWay}"
                            HorizontalAlignment="Stretch" />
                        <TextBlock Text="{Binding ActivePacName}" Opacity="0.5" FontSize="12" />
                    </StackPanel>
                </Border>
            </Grid>

            <InfoBar
                x:Uid="DashboardErrorBar"
                x:Name="ErrorInfoBar"
                Message="{Binding StateMessage}"
                IsOpen="{Binding IsErrorVisible}"
                Severity="Error"
                IsClosable="True" />
        </StackPanel>
    </ScrollViewer>
</Page>
